import React from 'react';

const Decay = (props: any) => {
    /**
     *  Core computation takes place here. Pathfinding, engine queries, movement, etc.
     */
    
    return { ...props.children };
};

export default Decay;
